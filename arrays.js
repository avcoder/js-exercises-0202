console.log("arrays.js");

// Create `myArray` so that it contains a list of 3 fruits

// Get length of array

// Access first/last array element with index

// Access a non-existent element with index

// Modify array elements with index

// Swap first and last elements in an array

// Modify array with push

// Modify array with pop

// Modify array with shift

// Modify array with unshift

// slice vs splice

// view other Array methods on MDN

// Use prompt to get a word and append/prepend to an existing array

// Use confirm to remove last/first element in an array

// Reverse the array (using indexes way)

// Encapsulate all array elements within a h1 tag
